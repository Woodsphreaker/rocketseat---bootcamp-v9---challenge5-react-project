import styled from 'styled-components'

const Title = styled.h1`
  font-size: 24px;
  color: #7159c1;
  font-family: Arial, Helvetica, sans-serif;

  small {
    font-size: 14px;
  }

  p {
    font-size: 10px;
  }
`

export { Title }
